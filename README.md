# OfflineMessagingApi

python3 --version
pip3 install pipenv
pipenv shell
pipenv install flask flask-sqlalchemy flask-marshmallow marshmallow-sqlalchemy flask-login

to run the project:
python app.py

to test:
pytest tests
